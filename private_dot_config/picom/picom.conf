# General Settings
backend = "glx";
vsync = true;

# Transparency
active-opacity = 1.0;
inactive-opacity = 1.0;
frame-opacity = 1.0;

# This is crucial for Alacritty + glx
use-damage = false;

# Corner rounding (optional, looks nice with bspwm)
corner-radius = 8;
